<% if(typeof errors != 'undefined') {%>
   <% errors.forEach(function(error) { %>
    <%= error.message %>
   <% }); %>
<% } %>

<% if(success_message != '') { %>
  <%= success_message %>
<% } %>

<% if(error_message != '') { %>
  <%= error_message %>
<% } %>

<% if(error != '') { %>
  <%= error %>
<% } %>